import React from 'react'

const Collection = ({data,
    emtyTitle,
    emptyStateSubtext,
    collectionType,
    limit,
    page,
    totalPages}:) => {
  return (
    <div>Collection</div>
  )
}

export default Collection